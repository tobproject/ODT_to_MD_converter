# ODT_to_MD_converter
This script converts files with the `.odt` (OpenDocument Text) extension to Markdown files (`.md`), automatically adding the creation date in YAML format for easier use on platforms like GitBook. It also allows you to insert custom HTML styles into the resulting Markdown to improve the visual format.
